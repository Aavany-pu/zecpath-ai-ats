def generate_scoring_documentation(scoring_modules):
    document = {
        "Title": "HR Interview AI - Scoring Logic",
        "Purpose": (
            "Document the scoring stages used by the HR Interview AI."
        ),
        "Scoring Modules": scoring_modules
    }

    return document